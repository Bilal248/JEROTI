from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='An smart OS which identify OS of any hardware by fetching its data',
    author='Bilal Ahmad Khan',
    license='',
)
